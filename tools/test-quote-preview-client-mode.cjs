const assert=require('node:assert/strict');const fs=require('node:fs');const source=fs.readFileSync('src/components/QuoteGeneratorWindow.tsx','utf8');const start=source.indexOf('{showPreview && (');const end=source.indexOf('</div>\n          )}',start);const preview=source.slice(start,end);assert.match(preview,/Fullscreen/);assert.doesNotMatch(preview,/>Print</);assert.doesNotMatch(preview,/>Save</);assert.doesNotMatch(preview,/>Close</);assert.match(source,/onDrop=.*addImagesToItem/);console.log('Quote client preview behavior passed.');
